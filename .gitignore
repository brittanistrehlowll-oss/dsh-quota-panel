node_modules/
*.log
.DS_Store
.chrome-*/
.edge-*/
# Local review workspace: it snapshots the repo and carries a whole browser
# profile (cookies included). Never publish it.
docs/review-*/
dist/
*.tgz
