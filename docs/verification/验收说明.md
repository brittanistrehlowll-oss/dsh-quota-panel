# v0.6.0 极简胶囊本地验收

日期：2026-09-11。此目录为当前双态胶囊成果；`docs/review-20260911/` 为早期审查及已弃用方案，不代表当前界面。

## 完成内容

- 收起 176×28px，只显示三个状态点和读数；展开 176×56px，只显示三列名称/读数，紧凑层完全隐藏。
- 胶囊主体和锁为两个同级原生按钮，共享一个胶囊外观，避免嵌套按钮与事件冲突。锁只在展开时出现。
- 实际鼠标验证解锁可拖、锁定后 x/y 均不变、再次解锁可拖；刷新后位置及锁状态恢复。
- 默认独立左下角，不依赖鲸息。初始左边距 24px，下边距 64px；手动位置优先，可选宿主挂载完整组件。
- 保留现有 Provider 配置/路由，修复真实危险读数被旧值覆盖、非法数值误转 0、上游错误正文回显。
- 样式拆出为包内 `lib/capsule.css`，旧卡片样式、DOM 和失效验证器已替换；旧验证入口转调同一实际行为测试。

## 验证证据

- `node scripts/test-page-script.mjs`：44 项通过，实际模型、配置、语法和合成安全回归。
- `node scripts/verify-route.mjs`：7 项离线路由检查通过，线上明确跳过；不读取真实凭据。
- `node scripts/verify-package.mjs`：18 项打包/解包检查通过；9 个包内文件，包含 CSS，解包后可导入且生成脚本可编译。
- `node scripts/verify-capsule.mjs`：37 项实际 Chromium 检查通过。详见 `browser-results.json`，包括鼠标、键盘、锁/位置恢复、两态互斥、危险值→断网→恢复、可选宿主、窄视口、reduced-motion。
- 浅深色在 80/100/125/150/200% CSS zoom 下检查文字边界与相互重叠；这不是操作系统缩放或真实浏览器菜单缩放的等价验收。
- `light-small.png`、`light-expanded.png`、`light-locked.png`、`dark-small.png`、`dark-expanded.png` 来自实际候选代码渲染，示例额度，不代表当前账户。

## 运行边界

候选源码、离线演示和安装包已更新。未重启、停止、改配或覆盖 3080/3082 现有 DSH 服务，未进行真实账户调用、插件安装或远端发布。因此本地 PASS 不代表已有浏览器页面已经加载 v0.6.0。

已安装宿主的最终验收仍需其实际加载该候选；可核对页面根节点 `#dsh-quota-panel` 的 `data-version` 是否为 `0.6.0`。当前公开配置接口不变。旧拖动位置键继续支持，锁状态键为 `dsh.quota.locked`。
